# Installation
```
apt update -y && apt upgrade -y && apt install git -y && apt install bash -y && cd $Home && git clone https://github.com/y-nabeelxd/levanter-installer && cd levanter-installer && chmod +x install.sh && clear && bash install.sh -y && cd $Home && rm -rf levanter-installer && rm nodesource_setup.sh
```
